name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read

jobs:
  gateway:
    name: Gateway checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Lint (if configured)
        run: |
          HAS_ESLINT_CONFIG=false

          if [ -f .eslintrc ] || [ -f .eslintrc.json ] || [ -f .eslintrc.js ] || [ -f .eslintrc.cjs ] || [ -f .eslintrc.yml ] || [ -f .eslintrc.yaml ] || [ -f eslint.config.js ]; then
            HAS_ESLINT_CONFIG=true
          elif [ -f package.json ] && grep -q '"eslintConfig"' package.json; then
            HAS_ESLINT_CONFIG=true
          fi

          if [ "$HAS_ESLINT_CONFIG" = true ]; then
            npm run lint
          else
            echo "No ESLint config found. Skipping lint."
          fi

      - name: Run tests
        run: npm test -- --passWithNoTests

  mobile:
    name: Mobile checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile_app
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Run tests
        run: flutter test
